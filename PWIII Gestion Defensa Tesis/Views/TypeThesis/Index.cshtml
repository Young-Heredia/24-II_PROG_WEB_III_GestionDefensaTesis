@model IEnumerable<PWIII_Gestion_Defensa_Tesis.Models.TypeThesis>

<h1>Thesis Types</h1>

<p>
    <a href="@Url.Action("Create")" class="btn btn-primary">Create New Thesis Type</a>
</p>

<form method="get" action="@Url.Action("Index")" class="mb-4">
    <div class="form-row align-items-center">
        <div class="col-sm-4">
            <input type="text" id="searchQuery" name="searchQuery" value="@ViewBag.SearchQuery" class="form-control" placeholder="Search by Name..." />
        </div>
        <div class="col-sm-3">
            <div class="form-check form-check-inline">
                <input class="form-check-input" type="radio" name="filter" id="active" value="active" @(ViewBag.Filter == "active" ? "checked" : "") onchange="this.form.submit();" />
                <label class="form-check-label" for="active">Show Active</label>
            </div>
            <div class="form-check form-check-inline">
                <input class="form-check-input" type="radio" name="filter" id="inactive" value="inactive" @(ViewBag.Filter == "inactive" ? "checked" : "") onchange="this.form.submit();" />
                <label class="form-check-label" for="inactive">Show Inactive</label>
            </div>
        </div>
    </div>
</form>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Registration Date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Name</td>
                <td>@item.registerDate.ToString("dd/MM/yyyy")</td>
                <td>
                    @if (ViewBag.Filter == "inactive")
                    {
                        <form asp-action="Reactivate" method="post" style="display:inline;">
                            <input type="hidden" name="id" value="@item.Id" />
                            <button type="submit" class="btn btn-success">Recover</button>
                        </form>
                    }
                    else
                    {
                        <a href="@Url.Action("Details", new { id = item.Id })" class="btn btn-info">Details</a>
                        <a href="@Url.Action("Edit", new { id = item.Id })" class="btn btn-warning">Edit</a>
                        <a href="@Url.Action("Delete", new { id = item.Id })" class="btn btn-danger">Delete</a>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
